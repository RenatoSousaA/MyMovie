<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
                android:id="@+id/nav_home"
                android:icon="@drawable/ic_home"
                android:title="@string/menu_home" />
        <item
                android:id="@+id/nav_gallery"
                android:icon="@drawable/ic_add_movie"
                android:title="@string/menu_new_movie" />
        <item
                android:id="@+id/nav_slideshow"
                android:icon="@drawable/ic_map"
                android:title="@string/menu_maps" />
        <item
                android:id="@+id/nav_tools"
                android:icon="@drawable/ic_list_movie"
                android:title="@string/menu_all_movies" />
    </group>

    <item android:title="Information">
        <menu>
            <item
                    android:id="@+id/nav_About"
                    android:icon="@drawable/ic_info"
                    android:title="@string/menu_about" />
        </menu>
    </item>

</menu>
